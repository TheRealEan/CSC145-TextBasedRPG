// Filename: "Enemies.cpp"
#include "Enemies.h"
using namespace std;

ChineseSpyPigeon::ChineseSpyPigeon(string name)
	: ChineseSpyPigeon(name, 70, 70, 70, 10, 10, 20) {
};

ChineseSpyPigeon::ChineseSpyPigeon(string name, int health, int stamina, int mana, int strength, int armor, int gold)
	: Enemy(name, health, stamina, mana, strength, armor, gold) {
};

// ------------------------------------------------------------------ //

ChineseWeatherBalloon::ChineseWeatherBalloon(string name)
	: ChineseWeatherBalloon(name, 70, 70, 70, 10, 10, 20) {
};

ChineseWeatherBalloon::ChineseWeatherBalloon(string name, int health, int stamina, int mana, int strength, int armor, int gold)
	: Enemy(name, health, stamina, mana, strength, armor, gold) {
};

// ------------------------------------------------------------------ //

DannyDevito::DannyDevito(string name)
	: DannyDevito(name, 70, 70, 70, 10, 10, 20) {
};

DannyDevito::DannyDevito(string name, int health, int stamina, int mana, int strength, int armor, int gold)
	: Enemy(name, health, stamina, mana, strength, armor, gold) {
};

// ------------------------------------------------------------------ //

Drake::Drake(string name)
	: Drake(name, 70, 70, 70, 10, 10, 20) {
};

// ------------------------------------------------------------------ //

Drake::Drake(string name, int health, int stamina, int mana, int strength, int armor, int gold)
	: Enemy(name, health, stamina, mana, strength, armor, gold) {
};

// ------------------------------------------------------------------ //

Goblin::Goblin(string name)
	: Goblin(name, 70, 70, 70, 10, 10, 20) {
};

Goblin::Goblin(string name, int health, int stamina, int mana, int strength, int armor, int gold)
	: Enemy(name, health, stamina, mana, strength, armor, gold) {
};

// ------------------------------------------------------------------ //

Harpy::Harpy(string name)
	: Harpy(name, 70, 70, 70, 10, 10, 20) {
};

Harpy::Harpy(string name, int health, int stamina, int mana, int strength, int armor, int gold)
	: Enemy(name, health, stamina, mana, strength, armor, gold) {
};

// ------------------------------------------------------------------ //

Hippie::Hippie(string name)
	: Hippie(name, 70, 70, 70, 10, 10, 20) {
};

Hippie::Hippie(string name, int health, int stamina, int mana, int strength, int armor, int gold)
	: Enemy(name, health, stamina, mana, strength, armor, gold) {
};

// ------------------------------------------------------------------ //

Merfolk::Merfolk(string name)
	: Merfolk(name, 70, 70, 70, 10, 10, 20) {
};

Merfolk::Merfolk(string name, int health, int stamina, int mana, int strength, int armor, int gold)
	: Enemy(name, health, stamina, mana, strength, armor, gold) {
};

// ------------------------------------------------------------------ //

Nickelback::Nickelback(string name)
	: Nickelback(name, 70, 70, 70, 10, 10, 20) {
};

Nickelback::Nickelback(string name, int health, int stamina, int mana, int strength, int armor, int gold)
	: Enemy(name, health, stamina, mana, strength, armor, gold) {
};

// ------------------------------------------------------------------ //

Ninja::Ninja(string name)
	: Ninja(name, 70, 70, 70, 10, 10, 20) {
};

Ninja::Ninja(string name, int health, int stamina, int mana, int strength, int armor, int gold)
	: Enemy(name, health, stamina, mana, strength, armor, gold) {
};

// ------------------------------------------------------------------ //

Protestor::Protestor(string name)
	: Protestor(name, 70, 70, 70, 10, 10, 20) {
};

Protestor::Protestor(string name, int health, int stamina, int mana, int strength, int armor, int gold)
	: Enemy(name, health, stamina, mana, strength, armor, gold) {
};

Orc::Orc(string name)
	: Orc(name, 70, 70, 70, 10, 10, 20) {
};

Orc::Orc(string name, int health, int stamina, int mana, int strength, int armor, int gold)
	: Enemy(name, health, stamina, mana, strength, armor, gold) {
};

// ------------------------------------------------------------------ //

Pirate::Pirate(string name)
	: Pirate(name, 70, 70, 70, 10, 10, 20) {
};

Pirate::Pirate(string name, int health, int stamina, int mana, int strength, int armor, int gold)
	: Enemy(name, health, stamina, mana, strength, armor, gold) {
};

// ------------------------------------------------------------------ //

Roomba::Roomba(string name)
	: Roomba(name, 70, 70, 70, 10, 10, 20) {
};

Roomba::Roomba(string name, int health, int stamina, int mana, int strength, int armor, int gold)
	: Enemy(name, health, stamina, mana, strength, armor, gold) {
};

// ------------------------------------------------------------------ //

Samurai::Samurai(string name)
	: Samurai(name, 70, 70, 70, 10, 10, 20) {
};

Samurai::Samurai(string name, int health, int stamina, int mana, int strength, int armor, int gold)
	: Enemy(name, health, stamina, mana, strength, armor, gold) {
};

// ------------------------------------------------------------------ //

Swiftie::Swiftie(string name)
	: Swiftie(name, 70, 70, 70, 10, 10, 20) {
};

Swiftie::Swiftie(string name, int health, int stamina, int mana, int strength, int armor, int gold)
	: Enemy(name, health, stamina, mana, strength, armor, gold) {
};

// ------------------------------------------------------------------ //

TeaKettle::TeaKettle(string name)
	: TeaKettle(name, 70, 70, 70, 10, 10, 20) {
};

TeaKettle::TeaKettle(string name, int health, int stamina, int mana, int strength, int armor, int gold)
	: Enemy(name, health, stamina, mana, strength, armor, gold) {
};

// ------------------------------------------------------------------ //

TrashCan::TrashCan(string name)
	: TrashCan(name, 70, 70, 70, 10, 10, 20) {
};

TrashCan::TrashCan(string name, int health, int stamina, int mana, int strength, int armor, int gold)
	: Enemy(name, health, stamina, mana, strength, armor, gold) {
};
